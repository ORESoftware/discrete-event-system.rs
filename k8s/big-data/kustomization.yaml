apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - spark.yaml
  - airflow.yaml
  - minio.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: des-big-data
    includeSelectors: true
