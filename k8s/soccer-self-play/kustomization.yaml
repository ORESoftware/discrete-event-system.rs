apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - job.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: soccer-learning
    includeSelectors: true
