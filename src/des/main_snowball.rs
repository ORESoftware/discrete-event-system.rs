//! Port of `src/des/main-snowball.ts`.
//!
//! 1:1 file move. The TypeScript source is a STUB (only TODO comments). As a
//! library crate this is a no-op `run()` entry point rather than a `fn main`.
//!
//! Intended (per the TS TODO): simulate a snowball via a growth differential
//! equation `R = dr/dt · dt` — an ODE integrator step.

// TODO: simulate a snowball - differential equation
// R = dr/dt * dt

/// Entry point. Empty placeholder, matching the stub TypeScript source.
pub fn run() {}
